// Variantes, tamaños y estados permitidos para el Button Core.

export const BUTTON_VARIANTS = {
  PRIMARY: 'primary',
  DANGER: 'danger'
};

export const BUTTON_SIZES = {
  SM: 'sm',
  MD: 'md'
};

// Estados posibles para futura extensión (por ejemplo, loading o disabled visual).
export const BUTTON_STATES = {
  DEFAULT: 'default'
};

